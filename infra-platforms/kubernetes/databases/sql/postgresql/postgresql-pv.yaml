apiVersion: v1
kind: PersistentVolume
metadata:
  name: postgresql16-pv-volume
  labels:
    type: local
spec:
  storageClassName: local-storage
  claimRef:
    name: postgresql16-pvc
    namespace: postgresql16-ns
  capacity:
    storage: 4Gi
  accessModes:
    - ReadWriteOnce
  local:
    path: /mnt/k8s-worker-node1/postgresql
  nodeAffinity:
    required:
      nodeSelectorTerms:
        - matchExpressions:
            - key: kubernetes.io/hostname
              operator: In
              values:
                - k8s-worker-node1